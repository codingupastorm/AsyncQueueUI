namespace AsyncQueueUI
{
    class Program
    {
        static void Main(string[] args)
        {
            // Roughly we would:

            // Create the task queue.

            // Start the task executor loop (this is a different thread).

            // Start the UI
        }
    }
}
